# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'
use_frameworks!

target 'Git Octopus' do
  pod 'Alamofire', '~> 4.7'
  pod 'KeychainAccess', '3.1.0'

  target 'Git OctopusTests' do
    inherit! :search_paths

  end

end
